[{"body":"Fixes https://github.com/llvm/llvm-project/issues/180713\r\n\r\n* This patch introduces vector shuffle lowering for X86 target, initial support includes zeroable shuffles and true floating point shuffle.\r\n* We try to factor out snippets common to SDAG and GISEL.\r\n\r\n\r\n","createdAt":"2026-06-02T08:23:45Z","number":201055,"title":"[X86][GISEL] Lower Vector shuffle ","url":"https://github.com/llvm/llvm-project/pull/201055"},{"body":"This only applies to 64 bit primaries, and it changes the default of reserving large sections of memory to creating the regions as needed over time.\r\n\r\nThe total region size is still limited, but a future change should allow the number of regions to increase as long as there is memory.\r\n\r\nThe other restrictions:\r\n- Pointer compaction is not currently usable if multi-regions is enabled.\r\n- Enabling random offset is not allowed with this option.","createdAt":"2026-08-04T21:21:11Z","number":214076,"title":"[scudo] Add support for multi-regions.","url":"https://github.com/llvm/llvm-project/pull/214076"},{"body":"Add LLVM IR intrinsics (and their SelectionDAG patterns) for the XCVsimd operations that have **no generic-IR form**, on RV32:\r\n\r\n- dot products (`dotup`/`dotusp`/`dotsp`) and the accumulating `sdot*`\r\n- complex multiply (`cplxmul_r`/`cplxmul_i`), `cplxconj`, `subrotmj`\r\n- `shuffle` / `shuffle2` / `shuffle.sci` (incl. the\r\n  `CV_SHUFFLE_SCI_B_PSEUDO` expanded in `RISCVExpandPseudoInsts`)\r\n- `pack` (`packhi`/`packlo`)\r\n- `extract`/`extractu`/`insert`\r\n\r\nThe element-wise operations (add/sub, min/max, and/or/xor, abs) are deliberately **not** here: they are expressed as native packed-vector IR and selected in the codegen PR #205836, per @lukel97's review. Shifts, avg and the comparisons follow later with their codegen.\r\n\r\nThis PR was originally the full XCVsimd intrinsic set; it has been carved down to just the non-representable operations after review.\r\n\r\n**Tests:** `xcvsimd.ll`.\r\n\r\nPrerequisite for the non-representable XCVsimd Clang builtins (#204881), which lower to these intrinsics.\r\n\r\nPart of the CORE-V (XCV) series; RFC:\r\nhttps://discourse.llvm.org/t/rfc-core-v-xcv-support-for-cv32e40p-clang-builtins-xcvsimd-intrinsics-and-generic-auto-selection/91111\r\n\r\nPer the LLVM AI tool policy, Claude (via Claude Code) assisted with this PR: drafting the implementation, the tests, and this description. I have reviewed and understand all of it, ran the tests locally, and I am accountable for the contribution. The commits also carry an Assisted-by: Claude trailer.","createdAt":"2026-06-19T18:37:19Z","number":204880,"title":"[RISCV][XCV] Add LLVM intrinsics for non-representable XCVsimd ops","url":"https://github.com/llvm/llvm-project/pull/204880"},{"body":"","createdAt":"2026-03-24T08:13:56Z","number":188194,"title":"refacotred shuffle","url":"https://github.com/llvm/llvm-project/pull/188194"},{"body":"This is split up from #93673 with passing tests, so they can be merged faster.","createdAt":"2024-06-08T14:21:25Z","number":94857,"title":"[llvm][test] Fix filecheck annotation typos [1.5/n]","url":"https://github.com/llvm/llvm-project/pull/94857"},{"body":"This is initial patch that handles most common cases. Generic implementation is still needed.\r\n\r\nPatch By: Fateme Hosseini","createdAt":"2026-01-03T02:17:06Z","number":174261,"title":"Add lowering for llvm.vector.splice.va","url":"https://github.com/llvm/llvm-project/pull/174261"},{"body":"This can convert any fixed-size structure into any other fixed-size structure, as if by store+load through memory. This is intended to help replace the common store+load patterns that are inserted for ABI requirements, as well as eventually serve as a basis for a replacement SROA which relies less on the alloca type for semantics.\r\n\r\nIn particular, this was motivated by a concern I had that SROA is more concerned about what which combinations of bitcasts it knows how to implement, than about the profitability of SROA. This limited its perceived accuracy as an full SROA pass, as well as lead to a particular reliance on more heuristics (canConvertValue) than may be intended based on the exact alloca type declared--such as whether it has the ability to rewrite an operation as an integer or an array as a vector, if it sees both appear. That predicate would still be required to check if there's any \"weirdness\" happening with treating the result as just bits (like having matching offsets of all TargetExt and of NonIntegralPointer address space equality, or if either is an SVE). But at least the pass won't be forced to decide up front if operation should be an integer or an array or other representation.\r\n\r\nThis implements a (hopefully) correct pass in all cases. However any moderately complicated poison inputs are currently known to be incorrectly handled in general in LLVM. The most recent discussion on these poison semantics seems to be that they are logically contradictory right now: the last recommendation I know of is that vector operations must never be cast to an iN type, since that spreads poison where it is unsound to spread it. However, many passes (including SROA upon which this was modeled) was never fixed not to cast to iN: https://discourse.llvm.org/t/rfc-killing-undef-and-spreading-poison/42843/87 Simple poison inputs are dealt with by the need to maintain original types whenever possible.\r\n\r\nTo make this pass more careful, this could always use the vector-to-vector transforms (using the gcd or i1 type), or could always insert freeze when the source is a vector and the destination is not. However, either case may be challenging for many users of this pass, since SROA optimizations may make many common patterns and ABIs miscompile (e.g. source type {i8, i8} getting passed as i16 or used in a 2-byte atomic, where any part of the original type was padding). The byte type has been proposed to help fix this, but not yet merged.\r\n  Sources:\r\n  - https://discourse.llvm.org/t/rfc-add-a-new-byte-type-to-llvm-ir/89522\r\n  - https://gist.github.com/georgemitenkov/3def898b8845c2cc161bd216cbbdb81f\r\n  - https://blog.llvm.org/posts/2025-08-29-gsoc-byte-type/\r\n\r\nNote that there's two extract/insert strategies provided here, based on whether llvm backends are more canonical to handle being given a full index or a tree merge, controlled by `const bool USE_FULL_IDX` for current/future testing.\r\n\r\nAssisted-By: Claude Sonnet 4.6 <noreply@anthropic.com>","createdAt":"2026-03-16T16:41:28Z","number":186835,"title":"[IRBuilder] add a general CreateLayoutReinterpretCast cast builder","url":"https://github.com/llvm/llvm-project/pull/186835"},{"body":"Since #184903, the bytecode is now used in the test. This caused a failure on Windows (e.g. https://lab.llvm.org/buildbot/#/builders/211/builds/6765), as we compile with PDB by default and PDBs don't have info about templates.\r\n\r\nWith this PR, we force DWARF debug info. On Windows that requires LLD to be used because of the long section names.","createdAt":"2026-03-06T13:00:16Z","number":185002,"title":"[LLDB] Compile bytecode test with DWARF and LLD","url":"https://github.com/llvm/llvm-project/pull/185002"},{"body":"Moved fixes for llvm from #91854, plus few additional.\r\n\r\nNot all tests passing CI, so some help required to fix them.\r\n\r\nSee also https://github.com/llvm/llvm-project/issues/93674 for list of some unfixed tests","createdAt":"2024-05-29T12:22:07Z","number":93673,"title":"[llvm][test] Fix filecheck annotation typos [1/n]","url":"https://github.com/llvm/llvm-project/pull/93673"},{"body":"blocked on #93673, actual changes in last commit only, other ones from previous.","createdAt":"2024-06-13T16:35:25Z","number":95433,"title":"[llvm][test] Fix filecheck annotation typos [2/n]","url":"https://github.com/llvm/llvm-project/pull/95433"},{"body":"Following up on our discussion in #169125, I sketched out an initial implementation (this version doesn't fully compile, need to do a fair amount of refactoring to handle the multi-level `VectorizableTrees`). Just wanted to check in with you before fully implementing this patch that this is a direction you are comfortable with.\r\n\r\n- when we attempt to `buildTree()` with a large number of elements (larger than a single vector), break up the elements into `MaxVF` sized chunks and build a separate tree for each of them\r\n- If trees are successfully created, then iterate through them and look for opportunities to optimize across the trees\r\n- If profitable, vectorize all trees.\r\n\r\nI played around with several approaches, but I think this is most advantageous since it can be extended to cases where the multiple trees come from separate chains.","createdAt":"2025-12-11T22:08:34Z","number":171917,"title":"[WIP][SLP] Forest Vectorization for Wide Chains","url":"https://github.com/llvm/llvm-project/pull/171917"},{"body":"basically a revisit of https://github.com/llvm/llvm-project/pull/178387/. The motivation is basically subreg liveness not being properly represented in MIR at all stages. This patch updates the original by only introducing implicit defs when there are actual uses (full reg or subreg). For some history also see \r\nhttps://github.com/llvm/llvm-project/pull/201004, \r\nhttps://github.com/llvm/llvm-project/pull/189153\r\n\r\noriginal patch by ; @abhigargrepo","createdAt":"2026-06-29T09:38:25Z","number":206442,"title":"[CodeGen] Mark the undefined sub-registers of a tuple as IMPLICIT_DEF in two address instruction pass","url":"https://github.com/llvm/llvm-project/pull/206442"},{"body":"Adds a condition to the `isShuffleMaskLegal` method to prevent `combineInsertEltToShuffle` from creating an illegal `vector_shuffle` after type legalization which leads to a crash.\r\n\r\nContext:\r\nThis is triggered when bitcasting a `v2f16` into a vector that type legalizes to a `v2i32`. This happens on architectures supporting packed `f32` operations (>= `sm_100`). In certain cases, this leads to `combineInsertEltToShuffle` creating a vector shuffle with `v4f16` for simplification. Since this happens after type legalization and `v4f16` is an illegal type, it leads to a crash.","createdAt":"2026-05-18T09:24:06Z","number":198259,"title":"[NVPTX] Fix illegal combineInsertEltToShuffle pattern","url":"https://github.com/llvm/llvm-project/pull/198259"},{"body":"Lower additional AArch64 NEON fused multiply-accumulate builtins in CIR.\r\n\r\nThis covers:\r\n\r\n* `BI__builtin_neon_vfma_v`: `vfma_f16`, `vfma_f32`, `vfma_f64`\r\n* `BI__builtin_neon_vfma_lane_v`: `vfma_lane_f16`, `vfma_lane_f32`, `vfma_lane_f64`\r\n* `BI__builtin_neon_vfmah_lane_f16`: `vfmah_lane_f16`\r\n* `BI__builtin_neon_vfmah_laneq_f16`: `vfmah_laneq_f16`\r\n* `BI__builtin_neon_vfmas_lane_f32`: `vfmas_lane_f32`\r\n* `BI__builtin_neon_vfmas_laneq_f32`: `vfmas_laneq_f32`\r\n\r\n`vfma_v` and `vfma_lane_v` reuse the corresponding quad FMA lowering paths. Vector lane forms splat the selected vector element, while scalar lane/laneq forms extract the selected lane before emitting `llvm.fma`.\r\n\r\nRepresent NEON `Poly128` as a 16-byte integer vector in CIR, matching classic CodeGen. This avoids an unrelated `Poly128` NYI when lowering the f16 laneq wrapper.\r\n\r\nAdd direct LLVM, CIR-to-LLVM, and CIR coverage for every listed ACLE wrapper in the existing fused-multiply tests. Tighten the moved LLVM checks so FileCheck captures are introduced where values are produced instead of inside `llvm.fma` calls.\r\n\r\nPart of #185382\r\nFollow-up to #202337","createdAt":"2026-06-19T12:57:16Z","number":204819,"title":"[CIR][AArch64] Lower vfma and scalar FMA lane builtins","url":"https://github.com/llvm/llvm-project/pull/204819"},{"body":"InstCombine canonicalizes reverses, including vp.reverses, by pulling them through binary ops: https://godbolt.org/z/cs4M1TsE3\r\n\r\nWe have a combine that converts vp.reverses of vp.loads into vp.strided.loads with a stride of -1, but it only matches the pattern `vp.reverse(vp.load)` directly.\r\n\r\nThis PR teaches the combine to look through binary ops for vp.loads so it can match the canonicalized form when there's a binary op that uses the vp.reverse. \r\n\r\nAll leaves must be either a vp.load or a splat, but it still will only convert one vp.load at a time. This combine isn't always profitable so we should eventually remove it when it's done somewhere that's cost model driven like the loop vectorizer or vector combine.","createdAt":"2026-06-24T11:18:17Z","number":205529,"title":"[RISCV] Fold vp.reverse of vp.load through binary ops","url":"https://github.com/llvm/llvm-project/pull/205529"},{"body":"Reverts llvm/llvm-project#199512\r\n\r\nLLVM Buildbot has detected a build error for this PR.","createdAt":"2026-05-27T08:15:04Z","number":199899,"title":"Revert \"[RISCV][CodeGen] Use vzip.vv for e64 interleave shuffles with Zvzip\"","url":"https://github.com/llvm/llvm-project/pull/199899"},{"body":"As we have discussed in a previous PR (https://github.com/llvm/llvm-project/pull/98087) here is an implementation using ScheduleDAG in the MCP.\r\n\r\nThis PR is not fully finished yet. I have not really done any precise benchmarking.\r\n\r\nThe only thing I have done is that, I have tested how much time does the generation of some regression tests take before my patch and after my path. I have not seen any increases there on my machine. But this is not a precise way of measuring. \r\n\r\nI have not updated all the tests yet. I have only somewhat checked the RISCV, AArch64, ARM, X86 and Thumb2 tests.\r\n\r\nCould you please take a quick look at this PR and give some feedback?\r\nIs this direction good? Should we continue with this? (Then I will try to do some compile time benchmarking and also update the tests).","createdAt":"2024-08-21T18:12:53Z","number":105562,"title":"[MCP] Move dependencies if they block copy propagation","url":"https://github.com/llvm/llvm-project/pull/105562"},{"body":"Allow e64 interleave shuffles to use the standard Zvzip `vzip.vv` lowering when the operand type is legal for Zvzip, just the same as what XrivosVizip already does.","createdAt":"2026-05-25T10:38:34Z","number":199512,"title":"[RISCV][CodeGen] Use vzip.vv for e64 interleave shuffles with Zvzip","url":"https://github.com/llvm/llvm-project/pull/199512"},{"body":"A later PR will propose updating the default code model, so this PR sets an explicit code model on tests that don't have it. This is strictly NFC, as we are just setting the existing code model.\r\n\r\n(Assisted by AI)","createdAt":"2026-05-22T03:31:57Z","number":199159,"title":"[NFC][PowerPC][AIX] add explicit code model to 64-bit AIX tests","url":"https://github.com/llvm/llvm-project/pull/199159"},{"body":"If a shuffle operand is not used (as indicated by the mask), replace it with `ub.poison`. This may make the value dead and enable additional DCE. Also replace the entire shuffle op with `ub.poison` if all selected values are poisoned.\r\n\r\nAssisted-by: claude-4.6-opus-high\r\n\r\n","createdAt":"2026-04-07T09:54:36Z","number":190763,"title":"[mlir][vector] Replace unused shuffle operands / results with poison","url":"https://github.com/llvm/llvm-project/pull/190763"}]
